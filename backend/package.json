{
  "name": "heroes-of-penta-backend",
  "version": "0.0.1",
  "description": "Server for NFT game Heroes of Penta",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build && rimraf ../build && mkdir -p ../build && cp dist/* ../build -r && cp ./package*.json ../build && cd ../build && npm ci --omit=dev",
    "test": "vitest",
    "start": "node dist/index.js"
  },
  "keywords": [
    "web3",
    "nft",
    "game"
  ],
  "author": "Thjarkgh",
  "license": "GPL-3.0-only",
  "dependencies": {
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "vitest": "^3.0.2"
  },
  "devDependencies": {
    "@types/compression": "^1.7.5",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0"
  }
}
